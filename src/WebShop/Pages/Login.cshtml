@page
@model LoginModel
@{
    ViewData["Title"] = "Login";
}

<h1>Should never see me!!!</h1>

@foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }